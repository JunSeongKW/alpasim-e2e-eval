# DriveSuprim assets

Run the preparation script with the local DriveSuprim checkout:

```bash
bash e2e_challenge/sample_submission_drivesuprim/scripts/prepare_assets.sh \
  /rhome/junhyeok/DriveSuprim
```

The generated `assets/drivesuprim` directory is ignored by git.
